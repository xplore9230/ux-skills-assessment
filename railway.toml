[build]
builder = "NIXPACKS"
buildCommand = "cd server_py && pip install -r requirements.txt"

[deploy]
startCommand = "cd server_py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

